Do not add debugging onto the app
Always fix for long term solutions
You have access to the data via the Supabase MCP

## Database Schema Rules
- ALWAYS use Supabase MCP to check current database schema before making any table changes
- Use `mcp_supabase_list_tables` to verify existing table structure before modifications
- Use `mcp_supabase_execute_sql` to query current data before making schema changes
- Use `mcp_supabase_apply_migration` for all DDL operations (CREATE, ALTER, DROP)
- Check `mcp_supabase_get_advisors` after any database changes to ensure no security issues
- Verify data integrity with `mcp_supabase_execute_sql` SELECT queries before and after changes

## GitHub Integration Rules
- ALWAYS use GitHub MCP to push changes instead of terminal git commands
- Use `mcp_Github_MCP_push_files` for committing and pushing multiple files
- Use `mcp_Github_MCP_create_or_update_file` for single file updates
- Create feature branches with `mcp_Github_MCP_create_branch` before making changes
- Use `mcp_Github_MCP_create_pull_request` for code reviews and merging

## Deployment Rules
- ALWAYS use Vercel CLI for live deployments to external URLs
- Use `vercel --prod` command to deploy to production
- Use `vercel` command for preview deployments
- Ensure all changes are pushed to GitHub before deploying
- Verify deployment status and URL after deployment

---
description:
globs:
alwaysApply: true
---
