FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install ALL dependencies (including dev)
RUN npm install

# Copy source code
COPY . .

# Expose Vite port
EXPOSE 8086

# Start dev server with host binding
CMD ["npx", "vite", "--port", "8086", "--host", "0.0.0.0"]

