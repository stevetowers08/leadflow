version: '3.8'

services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8086:8086"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_PORT=8086
    command: npm run dev

